version: "3.9"

services:
  backend:
    build:
      context: ./backend
    image: backend:1.0
    container_name: resume-backend
    env_file: .env
    ports:
      - "8001:8001"
    networks:
      - app-net
    restart: unless-stopped
    depends_on:
      - ollama

  frontend:
    build:
      context: ./frontend
    image: frontend:1.0
    container_name: resume-frontend
    env_file: .env
    environment:
      # Tell the frontend JS where the backend lives (public URL seen by browser)
      BACKEND_URL: ${BACKEND_URL:-http://localhost:8001}
    ports:
      - "8000:8000"
    depends_on:
      - backend
    networks:
      - app-net
    volumes:
      - ./backend/data/:/app/data:ro
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: resume-ollama
    ports:
      - "11434:11434"
    volumes:
      - ${HOME:-/home/kchiang}/.ollama:/root/.ollama
    networks:
      - app-net
    restart: unless-stopped


networks:
  app-net:
    driver: bridge
